# =============================================================================
# Training requirements — low_parallax_vggt
# Install with: pip install -r training/requirements.txt
# =============================================================================

# --- Core deep learning ---
torch>=2.0.0
torchvision>=0.15.0

# --- Data / numerics ---
numpy>=1.23.0
opencv-python>=4.7.0
Pillow>=9.0.0
scipy>=1.10.0

# --- Config / hydra ---
hydra-core>=1.3.0
omegaconf>=2.3.0
fvcore>=0.1.5          # LR scheduler (CompositeParamScheduler, CosineParamScheduler, etc.)

# --- I/O / utils ---
iopath>=0.1.9
wcmatch>=8.4           # glob pattern matching for frozen_module_names / checkpoint loading
huggingface-hub>=0.19.0

# --- LoRA fine-tuning ---
peft>=0.10.0

# --- Logging ---
tensorboard>=2.13.0
matplotlib>=3.7.0      # optional: used in track_util for debug visualisation

# =============================================================================
# Preprocessing only (prepare_rembrand_dataset.py + smooth_colmap_poses.py)
# Not needed on the training pod if preprocessing is done separately.
# =============================================================================
# OpenEXR>=3.0.0       # EXR depth frame reading (also installs Imath)
